services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "4173"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - coolify

networks:
  coolify:
    external: true
